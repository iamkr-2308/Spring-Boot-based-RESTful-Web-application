# Spring-Boot-based-RESTful-Web-application
The Program allows users to create, read, update, and delete (CRUD) journal entries via API endpoints.  It functions as a simple in-memory journal management system, where each entry has:  an ID a title a content body The application exposes RESTful endpoints that can be accessed using tools like Postman or curl, and it responds in JSON format.  
